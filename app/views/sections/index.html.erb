<% @page_title = "Section List" %>

<%= link_to("<< Back to Menu", {:controller => 'access', :action => 'index'}, :class => 'back-link') %>

<%= link_to("<< Back to Section Type", section_types_path, :class => 'back-link') %>

<div class="sections index">
  <h2>Sections</h2>
  
  <%= link_to("Add New Section", new_section_type_section_path(:section_type_id => @section_type.id), :class => 'action new') %>

  <div class="num-found"><%= pluralize(@sections.size, 'section')%> found</div>

  <table border="1" class="listing" summary="Sections list">
    <tr class="header">
		<th>Section type name tag</th>
      	<th>Position</th>
		<th>Section name tag</th>
		<th>Visible</th>
		<th>Content</th>
      	<th>Actions</th>
    </tr>
    <% @sections.each do |section| %>
    <tr class="<%= cycle('odd', 'even') %>">
		<td><%= section.section_type.name_tag if section.section_type %></td>
      	<td><%= section.position %></td>
		<td><%= section.name_tag %></td>
		<td class="center"><%= status_tag(section.visible) %></td>
		<td><%= section.content %></td>	
      	<td class="actions">
      		<%= link_to('Show', section_type_section_path(:id => section.id, :section_type_id => @section_type.id), :class => 'action show') %>			
      		<%= link_to('Edit', edit_section_type_section_path(:id => section.id, :section_type_id => @section_type.id), :class => 'action edit') %>
        	<%= link_to('Delete', delete_section_type_section_path(:id => section.id, :section_type_id => @section_type.id), :class => 'action delete') %>
      </td>
    </tr>
    <% end %>
  </table>
</div>